# Vendored example agent

`Local_Info_Agent.agent` is a copy of the `Local_Info_Agent` Agent Script file from the
Salesforce [AFDX Pro-Code Test Drive](https://github.com/forcedotcom/afdx-pro-code-testdrive)
sample (Apache-2.0, © 2024 Salesforce, Inc.).

It is vendored here so the example and the lint/sim tests are **self-contained** — they
run on a fresh clone with no external repos. See the top-level `NOTICE`.

To refresh it, re-copy from the upstream bundle at
`force-app/main/default/aiAuthoringBundles/Local_Info_Agent/Local_Info_Agent.agent`.
